import controller.TicTacToeController;import entity.Board;import entity.GameState;import entity.Player;import entity.TicTacToe;import java.util.ArrayList;import java.util.List;import java.util.Scanner;public class Client {    public static void main(String[] args) {        TicTacToeController controller = new TicTacToeController();        List<Player> players = new ArrayList<>();        Scanner sc = new Scanner(System.in);        System.out.println("Welcome To the Game");        System.out.println("Choose the Board dimension : ");        int dimension = sc.nextInt();        Board board = new Board(dimension);        // Board Ready        System.out.println("Choose the number of player : ");        int playerCount = sc.nextInt();        while (playerCount-- > 0) {            System.out.println("Enter the {0} Player Name : ");            String playerName = sc.next();            System.out.println("Choose Player symbol : ");            String symbol = sc.next();            players.add(new Player(playerName, symbol));        }        // player ready        TicTacToe ticTacToe = controller.initializeTheGame(players, board);        // Game is Going on        while (controller.getGameState(ticTacToe) == GameState.IN_PROGRESS) {            Player player = ticTacToe.getNextPlayer();            System.out.println("Its " + player.getName() + " turn");            controller.printTheBoard(ticTacToe);            System.out.println("Select Row : ");            int row = sc.nextInt();            System.out.println("Select Col : ");            int col = sc.nextInt();            controller.fillTheBoard(ticTacToe, row, col, player);        }        if (controller.getGameState(ticTacToe) == GameState.WIN) {            System.out.println(ticTacToe.getWinner() + " is winner");        } else {            System.out.println("Match Drawn !");        }    }}