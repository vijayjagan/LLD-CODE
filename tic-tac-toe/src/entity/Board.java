package entity;import java.util.ArrayList;import java.util.List;public class Board {    private final List<List<Cell>> boardList;    int dimension;    public Board(int dimension) {        this.dimension = dimension;        boardList = new ArrayList<>();        // Initialize the game         for (int i = 0; i < dimension; i++) {            boardList.add(new ArrayList<>());            for (int j = 0; j < dimension; j++) {                boardList.get(i).add(new Cell(i, j));            }        }    }    public int getDimension() {        return dimension;    }    public void printTheBoard() {        for (List<Cell> col : boardList) {            for (Cell cell : col) {                if (cell.cellState == CellState.EMPTY) {                    System.out.print(" |  |");                } else {                    System.out.print(" |" + cell.player.symbol.toUpperCase() + "|");                }            }            System.out.println();        }    }    public void fillTheMove(int row, int col, Player player) {        Cell cell = boardList.get(row).get(col);        if (cell.cellState == CellState.TAKEN) {        }        cell.setPlayer(player);    }}